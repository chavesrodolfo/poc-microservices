server.port: 8888

spring:
  application:
    name: config-server
    index: ${random.int}
  cloud.config:
    server:
      git.uri: https://github.com/chavesrodolfo/poc-microservices-config.git
      bootstrap: true
